var menu = function(app,path) { 
    
    /*app.routes['get']
    .find(function (element, index, array){
      return element.path.test('#/users');
    })
    .callback=function (c) {		      
		c.api('/subscriptions', function(data) {
            c.api('/users', function(data2) { 
                data['users']=data2['users']
                c.view([path,'subscription/subscription_list'], data);
            }); 
        });
    };
    
    app.get('#/subscriptions/valid/:subscription', function (c) {
        c.api('/subscriptions/'+c.params['subscription'], function(data) { 
            c.redirect('#/users');
        }, 'PUT');
    });
    
    app.get('#/subscriptions/remove/:subscription', function (c) {
        c.api('/subscriptions/'+c.params['subscription'], function(data) { 
            c.redirect('#/users');
        }, 'DELETE');
    });*/
  
};
